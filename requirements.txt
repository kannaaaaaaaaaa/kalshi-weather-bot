# Core
httpx>=0.27.0          # Async HTTP client
python-dateutil>=2.9   # Timezone handling

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# Dashboard
flask>=3.0          # Web dashboard for viewing results

# Future (not needed yet)
# aiosqlite>=0.20     # Async SQLite if we need it
# rich>=13.0          # Pretty terminal output
